#include <stdio.h>
#include <windows.h>

int main(){
    int ag = 0;
    int conta = 0;
    int senha = 0;
        printf("Bem vindo ao Hulema\nRealize Login\n")
        printf("Agencia: ");
        scanf("%d",&ag);
        printf("Conta: ");
        scanf("%d",&conta);
        printf("Senha: ")
        scanf("%d", &senha);
        login(ag, conta, senha);
    return 0;
}